#include "main.h"
/**
 * _strlen - function that returns
 *
 * Description: function that returns a length of string
 *
 * @s: pointer
 *
 * Return: success 0
 */
int _strlen(char *s)
{
int i;

for (i = 0; s[i] != '\0'; i++)
{


}
return (1);
}
